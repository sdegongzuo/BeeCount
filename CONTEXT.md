# Context

## Terms

- 图片规则升级样本：从项目外复制到评测目录的支付截图样本。它用于升级和验证 TOML 规则模板，不代表真实 Android 分享入口。
- 模拟来源 App 名称：图片规则升级样本缺少系统分享来源时，由评测夹具根据样本清单预先提供的 App 名称。它只用于规则匹配验证，不写入生产分享链路。
- 候选规则模板：由单张图片规则升级样本生成或调整的 TOML 模板。它必须经过现有评测验证后，才能成为内置规则模板。
- 规则升级离线工具：在 App 运行时之外使用的开发工具。它读取本地图片和模拟来源 App 名称，生成候选规则模板或补丁建议。
- 候选规则文件：规则升级离线工具产出的独立 TOML 文件。它不直接替换内置规则包，必须经确认、合并和评测后才能影响真实识别。
- 期望账单数据：用于评估候选规则模板的人工确认真值。它可以由工具预生成，但必须经用户核对确认后才作为规则升级依据。
- 规则升级用例文件：规则升级离线工具产出的待确认 JSON 文件。它汇总图片路径、模拟来源 App 名称、OCR 文本、现有规则结果、AI 建议结果和待确认的期望账单数据。
- AI 预填期望：由当前 App 配置的 AI 图片理解能力生成的期望账单数据草稿。它只能作为待核对内容，不能直接作为规则升级真值。
- 补充明细：交易主字段之外仍有用户阅读价值的账单信息，例如订单号、优惠、门店、路线或行程时间。它用于交易详情展示，不应包含调试元数据、重复主字段或无业务意义的 OCR 噪声。

### Billing Job 系统

**Billing Job**：一次图片分享记账请求的完整生命周期记录。持久化在 `billing_jobs` 表中，从图片接收到附件保存完成，经历 5 个幂等阶段。
_Avoid_：任务、job（在非 billing_job 语境下）

**Stage**：billing job 主流程的进度标记，表示"已经完成到哪一步"。恢复时从下一阶段继续，不重复已完成的工作。值：received / ocr_done / rule_done / transaction_created / ai_done。附件保存独立于主流程，用 `attachment_done` boolean 追踪。
_Avoid_：步骤、phase

**Retryable Failed**：job 的一种状态，表示当前阶段失败但可恢复。下次 App 回到前台时自动重试，从已完成阶段继续。
_Avoid_：暂时失败、可重试

**Lease Until**：billing job 的并发控制时间戳。防止多个 worker 同时领取同一个 job。最多允许 2 个 job 并发执行。
_Avoid_：锁、锁定期

**Foreground Service 保护窗口**：Android Foreground Service 提供的 90 秒执行保护期。在此窗口内系统不会杀死进程，超时后 job 进度已持久化，未完成阶段标记为 retryable_failed。
_Avoid_：后台服务窗口

**BillingJobRunner**：驱动 billing job 通过状态机的执行器。负责阶段调度、超时检测、错误分类和进度持久化。
_Avoid_：任务执行器
